version: "3.9"

services:
  api:
    build:
      dockerfile: Dockerfile.test
    environment:
      - ENVIRONMENT=test
    command: >
      python -m pytest -s --cov=app --cov-report=term-missing --cov-fail-under=90 --cov-report=html
    volumes:
      - .:/app